<!-- index.erb -->
<ul>
  <% Document.all.each do |document| %>
    <li>
      <span><a href="/<%= document.name %>"><%= document.name %></a></span>
      <span>
        <form class="button" method="get" action="<%= document.name %>/edit">
          <button type="submit">Edit</button>
        </form>
      </span>
      <span>
        <form class="button" method="post" action="/<%= document.name %>/duplicate">
          <button type="submit">Duplicate</button>
        </form>
      </span>
      <span>
        <form class="button" method="post" action="/<%= document.name %>/delete">
          <button type="submit">Delete</button>
        </form>
      </span>
      <span>
        <form class="button" method="get" action="/<%= document.name %>/versions">
          <button>Versions</button>
        </form>
      </span>
    </li>
  <% end %>
</ul>

<a href="/new">New Document</a>
<br />
<a href="/images">Images</a>

<br /><br />

<% if user? %>
  <form method="post" action="/users/signout">
    <em>Signed in as <%= session['user'] %>.</em>
    <button type="submit">Sign Out</button>
  </form>

  <form class="delete" method="post" action="/users/delete">
    <button type="submit">Delete This User</button>
  </form>
<% else %>
  <form method="get" action="/users/signin">
    <button type="submit">Sign In</button>
  </form>
<% end %>
